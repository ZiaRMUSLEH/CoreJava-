

                    STRING METHODLARI
1) toUpperCase()    :
                    i) Bir String deki tum harflari buyuk harfe cevirmek icin kullanilir.
                    ii) toUpperCase methodu "String" return eder.

2) toLowerCase()    :
                    i) Bir String deki tum harflari kucuk harfe cevirmek icin kullanilir.
                    ii) toLowerCase() methodu "String" return eder.

3) charAt( )        :
                    i) Bir String den belli bir index deki character i almak icin kullanilir.
                    ii) charAt() method u "char" return eder.

4) length()         :
                    i) bir datadaki karakter sayisini bize verir.
                    ii) int bir deger dondurur.

5) substring()      : iki kullanimi vardir
                    i) substring(baslangicIndexi,bitisIndex) i verilirse baslangic index dahil
                    bitis index haric yazdirir, Metnin ortasindan data almamizi saglar.
                    ii) (baslangicIndex) String in verilen indexten sonuna kadar alinmasini saglar
                    Bu method bize String dondurur.

6) contains()       :
                    i) Bir String de belli bir characterin veya characterlerin var olup olmadigini
                     anlamak icin kullanilir.
                    ii) contains() methodu "boolean" return eder.

7) startsWith()     :
                    Metnin bas kisminin dizi (1 veya daha fazla karakter) ile eslesip eslesmedigini
                    kontrol eder. Sonucu boolean dir.
